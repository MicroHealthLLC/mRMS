
<ul class="nav nav-pills mb-3 myTab" id="pills-tab" role="tablist">
  <li class="nav-item active">
    <a class="nav-link active my_reports" id="pills-All-tab" data-toggle="tab" href="#All_Reports" role="tab" aria-controls="pills-All" aria-selected="true">All</a>
  </li>
    <% ChannelEnum.all.each do |channelEnum| %>
    <li class="nav-item">
      <a class="nav-link my_reports" id="pills-<%= channelEnum.id %>-tab" data-toggle="tab" href="#reports<%= channelEnum.id %>" role="tab" aria-controls="pills-<%= channelEnum.id %>" aria-selected="true"><%= channelEnum.name%></a>
    </li>
    <% end %>
</ul>

<div class="tab-content show_reports" id="pills-tabContent" >
  <%= render partial: 'report_show', locals: { pivot_tables: pivot_tables} %>
</div>
