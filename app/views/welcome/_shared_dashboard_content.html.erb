<% if dashboards.present? %>
  <div class="row" id="<%= display_more_dashboard %>">
    <% dashboards.each do |dashboard| %>
      <%= link_to channel_multi_data_set_dashboard_path(dashboard.channel,dashboard) do%>
        <div class="card m-2 p-2 cursor-pointer" style="width: 20rem">
          <div class="text-center h-100">
            <div class="card-body pt-0 pb-2">
              <%= image_tag "dashboard.png", style: 'width: 13rem' %>
              <div class="cursor-pointer text-center">
                  <p title="<%= dashboard.name %>" class="text-black-50 px-3 mb-0 mt-2">
                  <%= dashboard.name %>
                </p>
              </div>
            </div>
          </div>
        </div>
      <% end %>
    <% end %>
    <div class="col-md-12 py-2 pb-5">
      <% if dashboard_count > 3 && dashboards.count == 3 %>
        <%= link_to 'Show More...', welcome_index_path("#{display_more_dashboard}": true), remote: true, class: 'pull-right' %>
      <% elsif dashboards.count > 3 %>
        <%= link_to 'Show Less...', root_path, remote: true, class: 'pull-right' %>
      <% end %>
    </div>
  </div>
<% else %>
  <div class="col-md-12">
    <div class="alert alert-custom alert-notice alert-light-primary show d-flex" role="alert">
      <div class="alert-icon"><i class="fas fa-exclamation-triangle"></i></div>
      <div class="alert-text">No Shared Dashboard to display!</div>
    </div>
  </div>
<% end %>
